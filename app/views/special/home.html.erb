<% feed_autodiscovery entries_path(:format => :rss) %>
<% add_column_class('home') %>




<% if !@closing_soon.empty? %>
  <div class="clear marginTop">
    <h2>Comments Closing Soon</h2>
    <ul class="indent marginTop raquo">
    <% @closing_soon.each do |date| %>
      <li><%= link_to date.entry.title, entry_path(date.entry) %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<% if !@recently_opened.empty? %>
  <div class="clear marginTop">
    <h2>Comments Recently Opened</h2>
    <ul class="indent marginTop raquo">
    <% @recently_opened.each do |date| %>
      <li><%= link_to date.entry.title, entry_path(date.entry) %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<% if !@recent_proposed_rules.empty? %>
<div class="clear marginTop">
  <h2>Newly Proposed Rules</h2>
  <ul class="indent marginTop raquo">
  <% @recent_proposed_rules.each do |proprule| %>
    <li><%= link_to proprule.title, entry_path(proprule)%></li>
  <% end %>
  </ul>
</div>
<% end %>

<% content_for :sidebar do %>

  <h3>View Entries Near</h3>
  <%= render :partial => 'maps/location_search_form', :locals => {:location => nil} %>

  <%= render :partial => 'agencies/pulse', :locals => {:featured_agencies => @featured_agencies} %>
<% end %>